# Development dependencies: testing and code-quality tools. Kept in sync
# with [project.optional-dependencies.dev] in pyproject.toml.
#
#   pip install -r requirements.txt -r requirements-dev.txt
# or equivalently:
#   pip install -e ".[dev]"
pytest==8.3.3
pytest-asyncio==0.24.0
black==24.10.0
ruff==0.7.4
mypy==1.13.0
types-PyYAML==6.0.12.20240917
